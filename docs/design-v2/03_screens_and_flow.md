# ACMS 設計 v2 — 画面と操作フロー

## 画面一覧

### A. ダッシュボード

| # | 画面名 | 概要 |
|---|---|---|
| A1 | ダッシュボード | 全サイト概況。未公開の新世代、最近のデプロイ |

### B. サイト管理

| # | 画面名 | 概要 |
|---|---|---|
| B1 | サイト一覧 | 全サイト。ステータス・最終公開日 |
| B2 | サイト詳細 | ページ一覧 + サイト設定 + デザイン設定へのリンク |
| B3 | サイト設定 | XServer接続・GAS URL・基本情報 |

### C. ページ管理

| # | 画面名 | 概要 |
|---|---|---|
| C1 | ページ一覧 | サイト内の全ページ。世代情報、公開状態 |
| C2 | ページ詳細 | 現在の世代のプレビュー + 世代履歴 + 「記事生成」ボタン |
| C3 | 世代比較 | 任意の2世代の差分表示 |
| C4 | 微細編集 | 現在の世代に対するWYSIWYG編集。保存時に差分+理由を記録 |
| C5 | プレビュー | デザインシステム適用済みの公開プレビュー |

### D. コンテンツ取り込み

| # | 画面名 | 概要 |
|---|---|---|
| D1 | 原稿取り込み | Google Docs/Drive URL を指定 → 内容を取得・プレビュー → 「取り込み」で世代として保存 |
| D2 | 取り込みプレビュー | 取得したHTMLのプレビュー。問題なければ取り込み確定 |

### E. 公開管理

| # | 画面名 | 概要 |
|---|---|---|
| E1 | 公開確認 | ready状態のページ一覧。まとめて or 個別に「公開」ボタン |
| E2 | デプロイ履歴 | 過去のデプロイ一覧。ロールバック可能 |

### F. デザインシステム

| # | 画面名 | 概要 |
|---|---|---|
| F1 | デザイントークン管理 | カラー・フォント・スペーシングの値をGUIで編集。リアルタイムプレビュー |
| F2 | コンポーネント一覧 | 全コンポーネントのプレビュー。スタイル変数の調整 |
| F3 | コンポーネント詳細 | 1コンポーネントのHTML構造確認 + スタイル微調整 |
| F4 | サイトデザイン設定 | サイト固有のトークン上書き + カスタムCSS |
| F5 | TOPレイアウトエディタ | TOPページのセクション並べ替え（将来実装） |

### G. 例外コンテンツ

| # | 画面名 | 概要 |
|---|---|---|
| G1 | 例外コンテンツ一覧 | 症例・医療広告GL配慮コンテンツ |
| G2 | 例外コンテンツ編集 | 人間入力 + AIブラッシュアップ切替 |

### H. ユーザー管理

| # | 画面名 | 概要 |
|---|---|---|
| H1 | ユーザー管理 | ユーザーCRUD + ロール設定 |

---

## 主要操作フロー

### フロー1: 原稿取り込み → 微調整 → 公開

```
① 記事生成（ACMS外）
   clinic-page-generator で記事を生成
   → Google Docs に原稿 + _markup.txt が出力される
   → 人間が内容を確認、必要ならDocs上で手直し
                         │
                         ▼
② 原稿指定（ACMS）
   管理者がACMSで対象ページを選択
   → 原稿取り込み画面(D1) で Google Docs URL を貼り付け
   → ACMS が Google Docs/Drive API で内容を取得
   → 取り込みプレビュー(D2) で確認
   → 「取り込み」ボタン
                         │
                         ▼
③ 世代として保存
   page_generations に新世代として保存
   source_url にDocs URLを記録（トレーサビリティ）
   status: received
                         │
                         ▼
④ 微調整（任意）
   プレビュー(C5) でデザイン適用後の見た目を確認
   │
   ├── 問題なし → status: ready
   │
   └── 微修正が必要 → 微細編集画面(C4)で修正
                         │
                         ▼
                    human_patch + patch_reason を記録
                    final_html を更新
                    status: ready
                         │
                         ▼
⑤ 公開
   公開確認画面(E1) → 「公開」ボタン
   → ビルドエンジン: final_html + デザインCSS → 完成HTML
   → FTPデプロイ → XServer
   → status: published, deploy_records に記録
```

### フロー2: コンテンツ更新（再取り込み）

```
DNA-OSの原本が更新 → clinic-page-generator で再生成
   → 新しい原稿がGoogle Docsに出力される
                         │
                         ▼
管理者がACMSで同じページに新しいDocs URLを指定
   → 新世代として取り込み
   → 旧世代は superseded に
                         │
                         ▼
管理者がプレビューで確認
   │
   ├── 前の世代のhuman_patchを参照可能
   │    （「前回はここを修正しましたが、新しい世代にも適用しますか？」）
   │
   ▼
微調整 → 公開（フロー1の④⑤と同じ）
```

### フロー3: ロールバック

```
「前の状態に戻したい」
    │
    ▼
デプロイ履歴(E2) → 対象のデプロイレコードを選択
    │
    ▼
「ロールバック」 → そのスナップショットを再デプロイ
    │
    ▼
page_generations の該当世代を published に戻す
現世代を rolled_back に
```

### フロー4: デザイン変更

```
デザイントークン管理(F1) → カラーを変更
    │
    ▼
リアルタイムプレビューで確認
    │
    ▼
「保存」 → site_designs を更新
    │
    ▼
コンテンツは変わらない。CSSだけ変わる
    │
    ▼
「再デプロイ」 → 全ページを新しいデザインでビルドし直し → FTP
```
